namespace Scaffold.Containers
{
    /// <summary>
    /// Marks a type as a config that can be registered and retrieved from ContainerConfig.
    /// </summary>
    public interface IConfig
    {
    }
}
