namespace Sample.Turn
{
    /// <summary>
    /// Service for managing the turn lifecycle (start turn, phase execution).
    /// </summary>
    public interface ITurnService
    {
        void StartTurn();
    }
}
