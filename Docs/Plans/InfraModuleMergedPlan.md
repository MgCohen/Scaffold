> *Generated by Claude 3.7 Sonnet on 2026-02-22 15:18*

# Infra Module — Merged Fix Plan

> [!NOTE]
> Consolidated plan merging [InfraModuleFixPlan.md](file:///c:/Users/user/Documents/Unity/Scaffold/Docs/Plans/InfraModuleFixPlan.md) and [infra-module-analysis.md](file:///c:/Users/user/Documents/Unity/Scaffold/Docs/Plans/infra-module-analysis.md), updated with user feedback. Per user instructions: Test folders are optional/omitted, Navigation module changes are restricted to flattening `Container/Runtime`, `Samples.asmdef` will be created, and other modules will be flattened.

---

## MVVM Module

### Module Structure
- Move `Data`, `Generators`, and `Prefabs` folders into a new `Assets` folder (`Assets/Scripts/Infra/MVVM/Assets/`).
- Move the `Extensions` folder into the `Runtime` folder as a nested namespace/folder.
- Create a `Container` folder with a DI installer for the module.
- Rename `Binding/Abstractions/` → `Binding/Contracts/`.

---

## Navigation Module

### Module Structure
- Flatten `Navigation/Container/Runtime` — move its contents directly into `Navigation/Container` and delete the empty `Runtime` folder. Update `.meta` files.

*(Note: Per user instructions, coding standard refactors for Navigation outlined in the analysis report are being skipped.)*

---

## NetworkMessages Module

### Module Structure
- Add the missing assembly definition to the `Samples` folder (`Scaffold.NetworkMessages.Samples.asmdef`).

---

## Events Module

### Module Structure
- Flatten `Events/Container/Runtime` — move its contents directly into `Events/Container` and update `.meta` files. Delete the now-empty `Runtime` folder.

---

## Containers Module
✅ Marked as completely clean. (No structural or coding violations remaining).
